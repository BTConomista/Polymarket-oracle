Righe da aggiungere alla tabella "Fonti esterne valutate in sessione"
(`docs/MANUALE_SOPRAVVIVENZA.md` §4). Il riferimento a "pista 20" nell'ultima
riga va aggiornato al numero pista reale assegnato quando si integra
`piste_pista20_da_integrare.md` in `docs/PISTE.md`.

| Opta / Stats Perform (Opta Data Feeds) | ❌ **chiusa, motivo commerciale non tecnico** (28/07/2026): nessun tier gratuito o self-serve, nessun pricing pubblicato, nessun trial — accesso solo via contratto di licenza enterprise (per competizione/paese/livello dato), pensato per broadcaster/club/betting operator. Non è un `robots.txt` da rispettare: è un prodotto a cui semplicemente non si accede senza un contratto commerciale |
| `whoscored.com` (dati Opta-derivati) | ⚠️ **non riproposta**: `robots.txt` non blocca esplicitamente le pagine match-centre (blocca solo `/Accounts/`, `/Predictions/`, `/Users/`), ma sono dati di terzi ridistribuiti senza licenza di riuso — non rientra nelle vie legittime già definite (fonti che ci consentono esplicitamente / API su licenza) |
| `diretta.it` / `flashscore.com` | ❌ **chiusa (28/07/2026)**: stessa piattaforma (Livesport s.r.o.). `robots.txt` non blocca le pagine risultati, ma è una SPA — i dati arrivano da una chiamata interna (`flashscore.ninja`) protetta da **anti-bot attivo** (token applicativo + fingerprint TLS di un browser reale): non aggirabile senza impersonare un client browser a livello di handshake, scelta di metodo esclusa (stessa regola "nessun aggiramento" di questa sezione). Playwright/Chromium pre-installato **non raggiunge comunque la rete pubblica in HTTPS in questa sessione** (`ERR_CONNECTION_RESET` anche su siti banali, limite ambientale indipendente dal sito). Dettaglio in [PISTE.md](PISTE.md) §3, pista 20 (numero da verificare all'integrazione) |
| `sofascore.com` | ❌ **conferma del 403 già noto**, e ora doppiamente chiusa: il `robots.txt` per `*` blocca esplicitamente i percorsi storici datati (`/*/2017-` … `/*/2025-`) — anche senza il 403 lo storico multi-stagione sarebbe comunque vietato da regola dichiarata |
